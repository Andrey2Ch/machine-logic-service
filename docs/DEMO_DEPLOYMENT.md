# 🚀 Деплой Демо-окружения Isramat

Пошаговая инструкция для развёртывания демо-версии системы для показа клиентам.

---

## ⚠️ ВАЖНО: Миграции БД

Перед любым деплоем и демонстрацией убедитесь, что все миграции применены.

Документация и правила: `docs/MIGRATIONS.md`

## 📋 Что получит клиент

| Компонент | URL |
|-----------|-----|
| **Дашборд** | https://demo.isramat.com (или Railway URL) |
| **TG бот** | @IsramatDemoBot |
| **Логин** | demo@isramat.com / Demo2024! |

---

## 🏗️ Архитектура

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Dashboard      │────▶│  MLS API        │────▶│  PostgreSQL     │
│  (Next.js)      │     │  (FastAPI)      │     │  (Demo DB)      │
│  Railway        │     │  Railway        │     │  Railway/Neon   │
└─────────────────┘     └─────────────────┘     └─────────────────┘
                               │
                               ▼
                        ┌─────────────────┐
                        │  TG Bot         │
                        │  (aiogram)      │
                        │  Railway        │
                        └─────────────────┘
```

---

## 📝 Шаг 1: Создание БД

### Вариант A: Railway PostgreSQL

```bash
# В Railway Dashboard:
# 1. New Project → Add PostgreSQL
# 2. Скопировать DATABASE_URL
```

### Вариант B: Neon/Supabase

```bash
# Создать проект на neon.tech или supabase.com
# Скопировать connection string
```

### Применение миграций и seed данных

```bash
# Подключиться к БД и выполнить:

# 1. Создать схему (из prisma)
npx prisma db push

# 2. Загрузить демо-данные
psql $DATABASE_URL -f scripts/seed_demo.sql
```

---

## 📝 Шаг 2: Деплой MLS (FastAPI)

### Railway

```bash
# 1. Fork репозитория machine-logic-service
# 2. Railway → New Project → Deploy from GitHub
# 3. Выбрать репозиторий
```

### Environment Variables

```env
DATABASE_URL=postgresql://user:pass@host:5432/demo_db

# Опционально
LOG_LEVEL=INFO
CORS_ORIGINS=https://demo.isramat.com
```

---

## 📝 Шаг 3: Деплой Dashboard (Next.js)

### Railway

```bash
# 1. Fork репозитория isramat-dashboard
# 2. Railway → New Project → Deploy from GitHub
# 3. Выбрать репозиторий
```

### Environment Variables

```env
# БД
DATABASE_URL=postgresql://user:pass@host:5432/demo_db

# MLS API
NEXT_PUBLIC_API_URL=https://mls-demo.up.railway.app
NEXT_PUBLIC_API_BASE_URL=https://mls-demo.up.railway.app

# NextAuth
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=https://demo.isramat.com

# OAuth (опционально)
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
```

---

## 📝 Шаг 4: Деплой TG бота

### Создание бота

1. Открыть @BotFather в Telegram
2. `/newbot` → `IsramatDemoBot`
3. Скопировать токен

### Railway

```bash
# 1. Fork репозитория TG_bot
# 2. Railway → New Project → Deploy from GitHub
```

### Environment Variables

```env
# Telegram
BOT_TOKEN=123456789:ABCDEFGH...
WEBHOOK_HOST=https://tg-demo.up.railway.app
WEBHOOK_PATH=/webhook

# API
FASTAPI_BASE_URL=https://mls-demo.up.railway.app

# Настройки
BOT_MACHINES_ROW_SIZE=4
LOG_LEVEL=INFO
```

---

## 📝 Шаг 5: Создание демо-пользователя в Dashboard

### Через NextAuth (email/password)

```sql
-- В БД добавить пользователя для дашборда
-- (NextAuth создаёт свои таблицы)

INSERT INTO users (email, name, role)
VALUES ('demo@isramat.com', 'Demo Admin', 'admin');
```

### Или использовать Google OAuth

Клиент входит через свой Google аккаунт, ему назначается роль admin.

---

## 🎯 Демо-аккаунты в системе

После выполнения `seed_demo.sql`:

| Роль | Имя | Factory Number | Telegram ID |
|------|-----|----------------|-------------|
| Admin | Демо Админ | DEMO-001 | 100000001 |
| Machinist | Иван Петров | DEMO-002 | 100000002 |
| Operator | Антон Сидоров | DEMO-004 | 100000004 |
| Warehouse | Елена Волкова | DEMO-007 | 100000007 |
| QA | Ольга Новикова | DEMO-008 | 100000008 |

### Как войти в TG бота (для демо)

1. Клиент пишет `/start` боту
2. Бот просит factory_number
3. Ввести `DEMO-001` (admin) или `DEMO-004` (operator)

---

## 🔧 Полезные команды

### Сброс демо-данных

```bash
# Очистить и перезагрузить демо-данные
psql $DATABASE_URL -c "TRUNCATE batches, lots, setup_jobs CASCADE"
psql $DATABASE_URL -f scripts/seed_demo.sql
```

### Проверка статуса

```bash
# MLS API
curl https://mls-demo.up.railway.app/health

# Dashboard
curl https://demo.isramat.com/api/health
```

---

## 📊 Что показывать клиенту

### 1. Dashboard (5-10 мин)

- [ ] Обзор производства (главная страница)
- [ ] Список лотов `/batches` — фильтры, статусы
- [ ] Аналитика качества `/quality-analytics`
- [ ] Admin панель `/admin` — станки, участки, пользователи

### 2. TG бот (5 мин)

- [ ] Войти как оператор (DEMO-004)
- [ ] Создать партию деталей
- [ ] Отправить на склад
- [ ] Показать уведомления

### 3. QA процесс (5 мин)

- [ ] Войти как ОТК (DEMO-008)
- [ ] Провести инспекцию партии
- [ ] Зафиксировать брак
- [ ] Показать статистику

---

## ⚠️ Важно

1. **Демо-токен бота** — не публикуй в GitHub!
2. **DATABASE_URL** — использовать переменные окружения
3. **Периодически сбрасывать данные** — чтобы демо было чистым
4. **Отдельный Railway проект** — не смешивать с продакшеном

---

## 🆘 Troubleshooting

### Бот не отвечает

```bash
# Проверить webhook
curl https://api.telegram.org/bot<TOKEN>/getWebhookInfo

# Установить webhook
curl https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://tg-demo.up.railway.app/webhook
```

### Dashboard 500 ошибка

```bash
# Проверить подключение к БД
railway logs -s dashboard

# Проверить env переменные
railway variables -s dashboard
```

### MLS API недоступен

```bash
# Проверить health endpoint
curl https://mls-demo.up.railway.app/health

# Смотреть логи
railway logs -s mls-api
```

---

## 📞 Текст для клиента

```
Привет! Вот доступ к демо-версии системы учёта производства:

🌐 Дашборд: https://demo.isramat.com
   Войти через Google или:
   Email: demo@isramat.com
   Пароль: Demo2024!

📱 Telegram бот: @IsramatDemoBot
   Для входа используйте код: DEMO-004 (оператор)
   или DEMO-001 (админ)

Там уже есть тестовые данные:
• 9 станков на 2 участках
• 8 лотов в разных статусах
• История производства за 2 недели
• Статистика по браку

Можете потестировать весь цикл:
создание партии → склад → ОТК → готово

Если будут вопросы — пишите!
```

---

*Последнее обновление: Январь 2026*


## 🎯 План создания Демо-окружения

[1 tool called]

Сначала проверю структуру БД чтобы создать правильный seed:

[2 tools called]

Создаю seed.sql с демо-данными:

[4 tools called]

## ✅ Готово! Создано для демо-окружения:

### 📁 Новые файлы:

| Файл | Описание |
|------|----------|
| `machine-logic-service/scripts/seed_demo.sql` | Демо-данные для БД |
| `machine-logic-service/docs/DEMO_DEPLOYMENT.md` | Полная инструкция деплоя |

---

### 📊 Что содержит seed_demo.sql:

| Данные | Количество |
|--------|------------|
| **Роли** | 6 (admin, operator, machinist, warehouse, qa, viewer) |
| **Участки** | 2 (CNC, Токарный) |
| **Станки** | 9 шт с настроенным порядком |
| **Сотрудники** | 9 (разные роли) |
| **Детали** | 8 чертежей |
| **Лоты** | 8 в разных статусах |
| **Партии (batches)** | 14 с историей движения |
| **Дефекты** | 3 записи для аналитики |
| **Типы материалов** | 8 |

---

### 🎯 Следующие шаги (твои действия):

#### 1. Создать демо-проект в Railway
```bash
# Новый проект "isramat-demo"
# Добавить PostgreSQL
# Применить seed:
psql $DATABASE_URL -f scripts/seed_demo.sql
```

#### 2. Создать демо-бота
```
@BotFather → /newbot → IsramatDemoBot
Скопировать токен
```

#### 3. Деплоить сервисы
- MLS API → Railway
- Dashboard → Railway  
- TG Bot → Railway

---

### 📱 Демо-доступы для клиента:

```
🌐 Дашборд: https://isramat-demo.up.railway.app
   Логин: demo@isramat.com / Demo2024!

📱 TG бот: @IsramatDemoBot
   Код оператора: DEMO-004

   Код админа: DEMO-001
```

---

Хочешь чтобы я помог с настройкой нового Railway проекта или созданием демо-бота?